package com.taxoryn.module.feedback.entity;

/** Internal triage priority. It is intentionally not exposed in customer APIs. */
public enum ApplicationFeedbackPriority {
    LOW,
    MEDIUM,
    HIGH,
    CRITICAL
}
