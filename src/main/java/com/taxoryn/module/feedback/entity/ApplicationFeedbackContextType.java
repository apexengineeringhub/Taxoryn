package com.taxoryn.module.feedback.entity;

/** Portal context derived by the service; never accepted from a feedback request. */
public enum ApplicationFeedbackContextType {
    CUSTOMER_PORTAL,
    PRACTICE_PORTAL
}
