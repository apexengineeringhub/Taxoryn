package com.taxoryn.module.feedback.entity;

/** Internal identity context derived from the authenticated platform user. */
public enum ApplicationFeedbackActorType {
    CUSTOMER,
    PRACTITIONER,
    PRACTICE_EMPLOYEE
}
